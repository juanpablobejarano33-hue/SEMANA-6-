info:
  name: POST2
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/incidencia
  body:
    type: json
    data: '{ "tipo": "Iluminación pública", "descripcion": "La comunidad reporta que una lámpara del parque no funciona desde hace varios días." } '
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
