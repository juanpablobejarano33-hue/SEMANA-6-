info:
  name: TRABAJO_POST
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/registro
  body:
    type: json
    data: |
      { 
        "nombre": "Maria", 
       "mensaje": "Hola comunidad"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
